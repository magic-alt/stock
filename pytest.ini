[pytest]
testpaths = tests
addopts = -v --tb=short --cov=src/optimizer --cov=src.strategies.ml_strategies --cov=src.strategies.ml_enhanced_strategy --cov-report=term-missing --cov-fail-under=90
markers =
    integration: integration tests
    slow: slow tests
