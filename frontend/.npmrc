# vue-router 5.3 declares Vite >=7.3 as an optional peer, while this app
# intentionally remains on the Vite 6 toolchain. npm 10 otherwise rejects
# the Dependabot-generated lockfile before lint/typecheck/build can run.
legacy-peer-deps=true
