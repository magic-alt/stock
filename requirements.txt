akshare>=1.12.0
pandas>=2.0.0
numpy>=1.24.0
PyYAML>=6.0.0
click>=8.0.0
rich>=13.0.0
sortedcontainers>=2.4.0
exchange_calendars>=4.5.0

# 核心依赖
backtrader>=1.9.76  # 回测框架
matplotlib>=3.5.0   # 图表绘制
psutil>=5.9.0      # 系统监控（可选，但推荐）

# 可选回测引擎（V3.3.0+）：启用 zipline 后端
# 取消下一行注释或 `pip install zipline-reloaded>=3.0` 安装；
# 未安装时框架仍以纯向量化模式运行 ZiplineBackend，CLI / GUI 可继续工作。
# zipline-reloaded>=3.0  # 可选：完整 zipline 模拟（C 扩展构建较慢）

# 测试依赖
pytest>=7.0.0
pytest-cov>=4.0.0

# 可选依赖
# tushare>=1.2.0      # 备用数据源（需要token）
# yfinance>=0.2.0     # 全球市场数据（可选）
pydantic>=2.0.0     # 配置验证
# structlog>=23.0.0   # 结构化日志（可选）
